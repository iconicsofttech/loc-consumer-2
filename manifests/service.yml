apiVersion: v1
kind: Service
metadata:
    name: "locdevnew-1fbc"
    labels:
        app: "locdevnew-1fbc"
spec:
    type: LoadBalancer
    ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
    selector:
        app: "locdevnew-1fbc"