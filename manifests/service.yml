apiVersion: v1
kind: Service
metadata:
    name: "locdevnew-261d"
    labels:
        app: "locdevnew-261d"
spec:
    type: LoadBalancer
    ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
    selector:
        app: "locdevnew-261d"