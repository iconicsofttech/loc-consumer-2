apiVersion: v1
kind: Service
metadata:
    name: "locdevnew-82d0"
    labels:
        app: "locdevnew-82d0"
spec:
    type: LoadBalancer
    ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
    selector:
        app: "locdevnew-82d0"