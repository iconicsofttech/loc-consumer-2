apiVersion: v1
kind: Service
metadata:
    name: "locdevnew-2579"
    labels:
        app: "locdevnew-2579"
spec:
    type: LoadBalancer
    ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
    selector:
        app: "locdevnew-2579"