apiVersion : apps/v1
kind: Deployment
metadata:
  name: "locdevnew-1fbc"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "locdevnew-1fbc"
  template:
    metadata:
      labels:
        app: "locdevnew-1fbc"
    spec:
      containers:
        - name: "locdevnew-1fbc"
          image: "csriconic.azurecr.io/locdevnew"
          ports:
          - containerPort: 3000