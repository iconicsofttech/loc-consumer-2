apiVersion : apps/v1
kind: Deployment
metadata:
  name: "locdevnew-2579"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "locdevnew-2579"
  template:
    metadata:
      labels:
        app: "locdevnew-2579"
    spec:
      containers:
        - name: "locdevnew-2579"
          image: "csriconic.azurecr.io/locdevnew"
          ports:
          - containerPort: 3000